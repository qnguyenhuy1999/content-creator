/**
 * Next.js recommended lint config for the landing app.
 * Avoids flat config patch resolution issues.
 */
module.exports = {
  extends: ["next/core-web-vitals"],
  ignorePatterns: [".next/**", "out/**", "build/**", "next-env.d.ts"],
};
module.exports = {
  root: true,
  extends: ["next/core-web-vitals"],
  ignorePatterns: [".next/**", "out/**", "build/**", "next-env.d.ts"],
};
