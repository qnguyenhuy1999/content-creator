# Root npmrc for pnpm workspace
# Keep a single shared lockfile for reproducibility
shared-workspace-lockfile=true
# Use hoisted node linker for better compatibility with tooling
node-linker=hoisted
# Use automatic package import method
package-import-method=auto
# Install peer deps automatically when possible
auto-install-peers=true
